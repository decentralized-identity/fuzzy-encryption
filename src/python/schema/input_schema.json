{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "description": "Parameters for generating a secret with the Fuzzy Key Recovery system",
    "type": "object",
    "properties": {
        "setSize" : { 
            "type": "number",
            "description": "number of words in the original and recovery word list"
        },
        "corpusSize": { 
            "type": "number",
            "description": "size of the set of words to choose from"
        },
        "correctThreshold": {
            "type": "number",
            "description": "minimum number of correct words need to unlock keys\nless than or equal to setSize"
        },
        "randomBytes": {
            "type": "array",
            "description": "array of upper case hexadecimal strings encoding random bytes",
            "items": {
                "type": "string",
                "pattern": "^[0-9A-F]+$"
            }
        }
    },
    "additionalProperties": false,
    "required": [
        "setSize",
        "corpusSize",
        "correctThreshold"
    ]
}
